Kept Oak's applicant browser usable during combat instead of closing the window automatically.
Disabled protected teleport, Mythic+ panel, Great Vault, and score-detail actions only while in combat inside an instance, with explanatory tooltips.
Restored Party Keys solo display by falling back to the player's owned keystone when LibOpenRaid has no party data.
Matched applicant invites more closely to Blizzard's convert-to-raid flow.
